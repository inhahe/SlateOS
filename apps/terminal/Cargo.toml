[package]
name = "terminal"
version = "0.1.0"
edition = "2024"
description = "OurOS Terminal Emulator — graphical VT100/xterm terminal"

[workspace]

[dependencies]
guitk = { path = "../../gui/toolkit" }

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
