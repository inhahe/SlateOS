[package]
name = "getopt"
version = "0.1.0"
edition = "2024"
description = "OurOS getopt/tsort/cksum - shell scripting helpers"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
