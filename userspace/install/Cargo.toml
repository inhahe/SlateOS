[package]
name = "install"
version = "0.1.0"
edition = "2024"
description = "OurOS install - copy files and set attributes"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
