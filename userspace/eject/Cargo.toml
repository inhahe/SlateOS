[package]
name = "eject"
version = "0.1.0"
edition = "2024"
description = "SlateOS eject utility"
[dependencies]
# The device path is an argv word, and the unknown-option message echoes the
# offending argument itself.
quoting = { path = "../quoting" }
