[package]
name = "acpi"
version = "0.1.0"
edition = "2024"
description = "OurOS acpi - power management info (acpi/acpid)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
