[package]
name = "getty"
version = "0.1.0"
edition = "2024"
description = "OurOS getty - virtual terminal login manager (agetty/mingetty)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
