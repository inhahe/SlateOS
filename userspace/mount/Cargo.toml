[package]
name = "mount"
version = "0.1.0"
edition = "2024"
description = "OurOS mount/umount — manage filesystem mount points"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
