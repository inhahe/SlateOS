[package]
name = "tekton-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# tekton-cli names pipelines, tasks and runs, identifiers created by whoever
# applied them to the cluster.
quoting = { path = "../quoting" }
