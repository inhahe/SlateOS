[package]
name = "conda-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# conda-cli names environments, packages and channels, taken from the
# command line and from downloaded index metadata.
quoting = { path = "../quoting" }
