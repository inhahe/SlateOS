[package]
name = "passwordgen"
version = "0.1.0"
edition = "2024"

[dependencies]
# The user's colours, per design-decisions 822.
appearance = { path = "../../gui/appearance" }
guitk = { path = "../../gui/toolkit" }

# The app-facing compositor client. An app names `oswindow` and never
# `guiremote`, for the reason a Unix program does not name the socket layer.
oswindow = { path = "../../gui/window" }
# The export writes a file of passwords; a half-written one looks complete.
safeio = { path = "../safeio" }

[lints]
workspace = true

[dev-dependencies]
# A directory to export into and read back.
scratchdir = { path = "../../userspace/scratchdir" }
