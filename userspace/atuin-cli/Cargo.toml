[package]
name = "atuin-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The search query and the rejected command name are argv words echoed back
# verbatim.
quoting = { path = "../quoting" }
