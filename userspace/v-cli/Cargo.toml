[package]
name = "v-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The new project's name and the v subcommand are argv words.
quoting = { path = "../quoting" }
