[package]
name = "ceph-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The subcommand name and the RBD image name are argv words echoed back.
quoting = { path = "../quoting" }
