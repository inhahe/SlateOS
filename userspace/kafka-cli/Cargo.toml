[package]
name = "kafka-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The topic name is an argv word, echoed by both the producer and the
# consumer banner.
quoting = { path = "../quoting" }
