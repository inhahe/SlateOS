[package]
name = "swupd-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The file searched for and the swupd subcommand are argv words.
quoting = { path = "../quoting" }
