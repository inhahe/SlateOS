[package]
name = "tor-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `prog` into "torsocks: routing {} through Tor SOCKS5
# 127.0.0.1:9050". That value is an argv word, so it is whatever the user
# typed and not text this program chose.
quoting = { path = "../quoting" }
