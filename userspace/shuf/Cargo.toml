[package]
name = "shuf"
version = "0.1.0"
edition = "2024"
description = "SlateOS shuf/factor/numfmt - randomization and number tools"
[dependencies]
# File names and option values come from argv, and with -e the input lines
# are themselves the words.
quoting = { path = "../quoting" }
