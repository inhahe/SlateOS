[package]
name = "kustomize-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# kustomize-cli names resources, images and overlay directories that come
# from the command line and from kustomization.yaml on disk.
quoting = { path = "../quoting" }
