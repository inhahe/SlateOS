[package]
name = "jira-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The target status of a `move` is an argv word, and the rejected command
# name is another.
quoting = { path = "../quoting" }
