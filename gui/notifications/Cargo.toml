[package]
name = "notifications"
version = "0.1.0"
edition = "2024"
description = "SlateOS Notification Daemon — system notification management and display"
[dependencies]
guitk = { path = "../toolkit" }
