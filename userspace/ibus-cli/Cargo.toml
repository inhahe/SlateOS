[package]
name = "ibus-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The input-engine name it switches to and the rejected command name are
# argv words.
quoting = { path = "../quoting" }
