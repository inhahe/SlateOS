[package]
name = "airflow-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# airflow-cli echoes the subcommand and the DAG and task names it was handed
# on the command line; every word it names back came from argv.
quoting = { path = "../quoting" }
