[package]
name = "guix-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The `guix system` action and the top-level subcommand it echoes back are
# argv words.
quoting = { path = "../quoting" }
