[package]
name = "ansible-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `role` into "- downloading role {} ...". That value is an
# argv word, so it is whatever the user typed and not text this program
# chose.
quoting = { path = "../quoting" }
