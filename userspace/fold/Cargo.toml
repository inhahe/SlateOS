[package]
name = "fold"
version = "0.1.0"
edition = "2024"
description = "SlateOS line-folding utility"
[dependencies]
# fold names the files it is wrapping, taken from the command line.
quoting = { path = "../quoting" }
