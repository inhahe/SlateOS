[package]
name = "dapr-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# dapr-cli names app IDs, methods, topics and pubsub components, every one
# of them registered by another process.
quoting = { path = "../quoting" }
