[package]
name = "servicebus"
version = "0.1.0"
edition = "2024"
description = "OurOS Service Bus — named service registry with typed RPC"

[workspace]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
