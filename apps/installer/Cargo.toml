[package]
name = "installer"
version = "0.1.0"
edition = "2024"
description = "SlateOS Installer — unattended and interactive installation"

[build-dependencies]
embed-manifest = "1.5.0"

[lib]
name = "installer"
path = "src/lib.rs"

[[bin]]
name = "installer"
path = "src/main.rs"
