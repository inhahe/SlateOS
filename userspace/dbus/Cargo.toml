[package]
name = "dbus"
version = "0.1.0"
edition = "2024"
description = "OurOS dbus - D-Bus message bus daemon (dbus-daemon/dbus-send/dbus-monitor)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
