[package]
name = "protobuf-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `subcmd` into "buf: command {} completed". That value is
# an argv word, so it is whatever the user typed and not text this program
# chose.
quoting = { path = "../quoting" }
