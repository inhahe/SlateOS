[package]
name = "chown"
version = "0.1.0"
edition = "2024"
description = "OurOS file ownership and permission utility (chown/chmod)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
