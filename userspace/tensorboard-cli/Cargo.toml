[package]
name = "tensorboard-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The tag being exported is an argv word, and the rejected command name is
# another.
quoting = { path = "../quoting" }
