[package]
name = "head"
version = "0.1.0"
edition = "2024"
description = "OurOS head - output first part of files"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
