[package]
name = "cargo-make-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `task` into "[cargo-make] INFO - Task {} completed". That
# value is an argv word, so it is whatever the user typed and not text this
# program chose.
quoting = { path = "../quoting" }
