[package]
name = "perf"
version = "0.1.0"
edition = "2024"
description = "SlateOS performance monitoring tool"
[dependencies]
# It interpolates `other` into "perf: unknown command {}". That value is an
# argv word, so it is whatever the user typed and not text this program
# chose.
quoting = { path = "../quoting" }
