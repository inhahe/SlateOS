[package]
name = "supabase-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# supabase-cli names projects, migrations and functions, taken from the
# command line and from the project directory on disk.
quoting = { path = "../quoting" }
