[package]
name = "libservicebus"
version = "0.1.0"
edition = "2024"
description = "OurOS service bus client library — connect to services, call methods, subscribe to signals"

# Standalone crate — not part of the kernel workspace.
[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
