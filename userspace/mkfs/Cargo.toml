[package]
name = "mkfs"
version = "0.1.0"
edition = "2024"
description = "OurOS mkfs — create filesystems on disk devices or image files"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
