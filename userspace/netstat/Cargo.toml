[package]
name = "netstat"
version = "0.1.0"
edition = "2024"
description = "OurOS network connection and socket statistics"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
