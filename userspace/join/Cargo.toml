[package]
name = "join"
version = "0.1.0"
edition = "2024"
description = "OurOS join utility -- join lines of two sorted files on a common field"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
