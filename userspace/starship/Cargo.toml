[package]
name = "starship"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The prompt-module name and the rejected command name are argv words.
quoting = { path = "../quoting" }
