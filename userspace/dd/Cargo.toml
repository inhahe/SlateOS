[package]
name = "dd"
version = "0.1.0"
edition = "2024"
description = "SlateOS data copy and conversion utility"
[dependencies]
# Operand words such as if= and of= come from argv, and the values are file
# names.
quoting = { path = "../quoting" }
