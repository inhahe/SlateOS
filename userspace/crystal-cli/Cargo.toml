[package]
name = "crystal-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The subcommand names crystal and shards echo back are argv words.
quoting = { path = "../quoting" }
