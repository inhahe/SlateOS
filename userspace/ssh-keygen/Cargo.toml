[package]
name = "ssh-keygen"
version = "0.1.0"
edition = "2024"
description = "OurOS SSH key generation and management"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
