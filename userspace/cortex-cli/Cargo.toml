[package]
name = "cortex-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The rules subcommand and the top-level subcommand it echoes back are argv
# words.
quoting = { path = "../quoting" }
