[package]
name = "pass-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# pass-cli names password-store entries and the source and destination of a
# move or copy, every one of them a path the user typed.
quoting = { path = "../quoting" }
