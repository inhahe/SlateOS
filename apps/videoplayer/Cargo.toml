[package]
name = "videoplayer"
version = "0.1.0"
edition = "2024"

[dependencies]
# The user's colours, per design-decisions 822.
appearance = { path = "../../gui/appearance" }
guitk = { path = "../../gui/toolkit" }
oswindow = { path = "../../gui/window" }

# What a video file holds -- its container, length and tracks -- read from
# its headers. There is no decoder, so this is what opening a file can do.
mediaprobe = { path = "../mediaprobe" }

[lints]
workspace = true
