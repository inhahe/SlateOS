# Fields written in production and read only by tests -- the known ones.
#
# WHAT THIS FILE IS FOR. `check-fields-written-never-read.py` found 26 of these
# on the day it was written. A gate that reports 26 things is a gate nobody
# reads, and two of the 26 were genuine user-facing defects that would have
# been lost in the noise -- `apps/fileassoc`'s `last_export` (an Export button
# that wrote nothing anywhere) and `apps/explorer`'s `eta_secs` (a time
# remaining computed on every tick and shown to nobody). Both are fixed.
#
# The remaining 25 are listed here so the gate is green today and RED the
# moment a twenty-sixth appears. This is a triage queue, not an amnesty: every
# line is a program doing work it then throws away, and each needs looking at
# on its own terms. Some will be sound -- a field kept for a future consumer,
# or read somewhere this script's textual matching cannot see. Those should get
# a `#` note here saying so, rather than being deleted.
#
# Delete a line when the field is fixed. Do not regenerate the file wholesale:
# the notes are the point, and a regenerating flag in a sibling script silently
# destroyed hand-written prose three times before it was made to refuse.
#
# Format: <path>:<field name>, one per line. `#` starts a comment.
#
# `apps/reminders/src/main.rs:completed_at` was here and was a FALSE
# POSITIVE. It is written into the program's JSON by
# `let Self { .., completed_at, .. } = self;` -- a destructuring pattern
# reads every field it binds, and binds them by bare name, so there is no
# `.field` anywhere for a textual detector to see. Fixed, with a fixture.
#
# A line here that no longer matches anything is FATAL, not harmless: it
# would silently suppress that field if it came back. Delete it when the
# field is fixed.
#
# `apps/hearts/src/main.rs:hearts_broken` was here and was a FALSE POSITIVE.
# The detector read the `=>` of a match arm as an assignment, so
# `None if !self.hearts_broken =>` -- the rule the game is named after,
# gating whether a heart may be led -- counted as a write rather than a
# read. The field then had no production reads and was reported as
# thrown-away work when it was load-bearing logic. Fixed, with a fixture.
#
# Noticed on triage and worth a closer look first:
#   gui/desktop/src/sound_settings.rs:output_device_id -- picking an output
#     device for an app records the choice and nothing acts on it. Deferred
#     only because design-decisions 815 moves this whole panel into
#     `apps/settings`, and fixing the copy that is about to be deleted is
#     work done twice. The port must not carry it over.
#   apps/netmanager/src/main.rs:diagnostics_running -- TRIAGED 2026-09-14, and
#     it is not a drawing bug. `run_diagnostics` sets it true, builds six
#     canned results, and sets it false again before returning, so no renderer
#     can ever observe it as true. Drawing a spinner for it would be theatre.
#     The flag becomes meaningful only when the diagnostics stop being
#     simulated and acquire a real wait; until then it is dead state that a
#     future async version will want, so it is kept and annotated rather than
#     deleted.
#   gui/desktop/src/lib.rs:shortcut_message -- a message composed for the user
#     and never rendered.

# TRIAGED 2026-09-14, BOTH REAL, neither fixed yet.
#
#   `test_results` (archivemanager): RE-TRIAGED, and the first note was
#   wrong. It said Test Archive "answers 3 files failed and cannot say
#   which". It can: `summary()` sorts the failures, names the first by
#   path, appends its reason for a `Corrupted(why)`, and adds "(and 2
#   other files did too)". So the user gets a name, a reason and a count
#   from one line, which is a good status line.
#
#   The real gap is narrower than claimed: with several failures you
#   learn one name, not the list. That matters for an integrity test --
#   knowing exactly which entries are bad is how you decide what can
#   still be recovered -- but it is a second surface for a program that
#   has one status line, not a missing wire. Left as a considered
#   omission rather than a defect; if a detail view is ever added, the
#   data is already here and complete.
#
#   `last_copied` (credmanager): FIXED 2026-09-14 -- the toolbar now says
#   what was copied and counts down to the clipboard wipe.
apps/archivemanager/src/main.rs:test_results
# FIXED 2026-09-14 by deletion. Kept for the reasoning:
#   `Store::refresh_group_counts` walks every contact to fill
#   `group.member_count`, and it is called in production (once). Nothing
#   reads it. The number the user sees comes from `Store::group_stats`,
#   which recomputes the SAME count with the identical filter on every
#   draw -- `contacts.iter().filter(|c| c.groups.contains(&g.id)).count()`
#   appears twice, ten lines apart in behaviour.
#
#   So this is two predicates for one state, which this tree has already
#   been bitten by (see the `can_clean` comment in diskcleanup: a button
#   that looked disabled and acted enabled). Today they agree. They will
#   stop agreeing the first time someone changes what group membership
#   means and finds only one of the copies.
#
#   Fix: delete `member_count` and `refresh_group_counts`, keeping
#   `group_stats`, which is always fresh and is the one already drawn. A
#   cached count that has to be refreshed by hand is the stale half of a
#   pair waiting to happen.
# TRIAGED 2026-09-14, SOUND. `affected_rows` duplicates a count that the
# `message` field beside it already states in words -- every producer sets
# both, as `QueryResult::success(&format!("{n} row(s) updated"))` with
# `affected_rows = n` on the next line. The user is told. This is the
# structured form for a consumer that does not exist yet (a script runner,
# an API), so drawing it would print the same number twice.
apps/dbviewer/src/main.rs:affected_rows
# TRIAGED 2026-09-14 (diskcleanup). `last_accessed_days` is FIXED -- the
# file preview now shows "45 days ago" beside each size, which is the only
# thing on the row that speaks to whether you want the file, and what this
# program does with the answer is `remove_dir_all`. The other two stay:
#
#   `min_age_days` is on `ScheduledCleanup` ("only clean items older than
#   this many days"), and the whole scheduled-cleanup feature is
#   unimplemented: `AppState.schedule` is `Option<ScheduledCleanup>`, set
#   to `None` at construction, and `.schedule` has ZERO references
#   anywhere else in the program. So this is an island, not a rule being
#   ignored -- worth being sure of, because an automatic cleanup that
#   silently dropped its age limit would delete files a user just made.
#
#   `path_pattern` records the glob that matched an item. The category is
#   already shown and the pattern is derived from it (`default_pattern`),
#   so drawing it would add a column that repeats the heading. Low value,
#   not wrong.
apps/diskcleanup/src/main.rs:path_pattern
apps/diskcleanup/src/main.rs:min_age_days
# TRIAGED 2026-09-14, REAL and BLOCKED. See known-issues.md ->
# TD-C-FOUR-CLIPBOARDS-AND-NONE-OF-THEM-IS-CONNECTED. `last_selected` holds
# the tinted emoji the user picked and is documented "for clipboard / IPC
# output"; there is no clipboard or IPC code in that program, no client
# library for the clipboard service anywhere, and A-Q15 means a second
# socket would kill the program's own window. So the emoji picker cannot
# give you an emoji, and the field is the last correct step before a gap.
apps/emojipicker/src/main.rs:last_selected
apps/netmanager/src/main.rs:diagnostics_running
# TRIAGED 2026-09-14, ISLAND. `sync_clipboard` sets this and is called only
# from its own tests, so remotedesktop's whole clipboard-sync feature has no
# production caller and its settings panel shows "Never / Text / 0" for
# ever. Adding the missing Size row would print a permanent zero, which is
# worse than the omission. This is the fifth disconnected clipboard --
# see TD-C-FIVE-CLIPBOARDS-AND-NONE-OF-THEM-IS-CONNECTED.
apps/remotedesktop/src/main.rs:content_size_bytes
# FIXED 2026-09-14 -- the label is drawn above the bar during a drag.
# Kept for the reasoning, which is the useful part:
#   Set on press, followed on every move, cleared on release -- a DRAG,
#   not a hover; the first version of this note said hover and was wrong.
#   Nothing draws it, and that is the missing half of a deliberate design:
#   the press arm's own comment says the picture only moves when the drag
#   is let go "so dragging across a film does not seek to every pixel of
#   the way there". Not seeking continuously is only tolerable because the
#   preview tells you where you are. Without it you drag blind and find
#   out where you landed by arriving there.
# TRIAGED 2026-09-14 -- THE SETTINGS-PANEL GROUP, and this is a checklist
# for the design-decisions 815 port rather than a list of things to wire
# where they stand. Fixing a panel that is about to be deleted is work done
# twice; carrying its bug across is work done wrong. Each of these is a
# value the panel computes and never shows or acts on:
#
#   backup_settings:last_backup_timestamp -- when the last backup ran is
#     recorded from the history entry and never displayed, so the one
#     question a backup screen exists to answer ("am I backed up?") is the
#     one it does not answer.
#   bluetooth:discoverable -- `set_discoverable` returns true and sets the
#     field, and NOTHING reads it. The toggle does not make the adapter
#     discoverable and the UI does not show that it is. A switch that does
#     nothing, in both directions.
#   startup_settings:last_run_at -- when a startup program last ran,
#     recorded and never shown, so a program that fails at every boot looks
#     the same as one that ran a second ago.
#   sound_settings:output_device_id, volume_override -- per-app audio
#     routing and volume, recorded and never applied.
#   lib.rs:shortcut_message -- FIXED 2026-09-14. It is drawn on the shortcut
#     card now. Kept here for the reason: the whole of the shortcut editor's
#     feedback was composed and drawn nowhere, so rebinding a key was silent
#     whether it worked or not -- including the case where it worked and could
#     not be saved.
#
gui/desktop/src/backup_settings.rs:last_backup_timestamp
gui/desktop/src/bluetooth.rs:discoverable
# FIXED 2026-09-14 by deletion, as was the contacts one. Kept for the
# reasoning, which is the pattern:
#   `record_invocation` sets `loading_timeout = self.response_time_avg_ms >
#   200.0`, and `is_slow()` twelve lines below returns
#   `self.response_time_avg_ms > 200.0`. The same predicate, cached in one
#   place and computed live in the other; only the live one is used.
#
#   `apps/contacts` has the identical shape (`member_count` cached,
#   `group_stats` recomputed, only the recomputed one drawn). Two in one
#   sweep makes it a pattern worth naming: a cached copy of a predicate
#   that has to be refreshed by hand is the stale half of a pair waiting
#   to happen, and this gate finds them because the cache is what nobody
#   reads. Fix in both cases is a deletion, keeping the live computation.
gui/desktop/src/sound_settings.rs:volume_override
gui/desktop/src/sound_settings.rs:output_device_id
gui/desktop/src/startup_settings.rs:last_run_at
# FIXED 2026-09-14 -- the dragged glyph is drawn at alpha 110. Kept for the
# reasoning:
#   `DragSource::show_ghost` is documented "whether the icon should appear
#   semi-transparent (drag in progress)" and `dragging_key` says which one.
#   Both are set when the drag threshold is crossed and cleared on drop or
#   cancel, and nothing reads either. `with_alpha` appears in the shell
#   only for taskbar and overlay transparency settings, never for a
#   dragged icon.
#
#   So dragging a tray icon or a taskbar pin gives no sign that anything
#   is being dragged: the icon sits where it was, at full opacity, while
#   the drop target changes under the pointer. The state machine is
#   complete and correct -- threshold, cancel-on-Escape, key rather than
#   index so a row can rearrange mid-drag -- and none of it is visible.
#   Fix: draw the icon named by `dragging_key` with `with_alpha` while
#   `show_ghost`. Small, and in lane C's own tree.
# TRIAGED 2026-09-14, SOUND. Both of these are library API with no
# consumer yet, which is the island problem rather than this one.
#
#   `attempts` is parsed from `options attempts:N` in resolv.conf. This
#   crate has no retry loop and no transport -- it encodes and decodes
#   DNS messages and parses the config -- so applying the count belongs
#   to whoever sends the query. The same is true of `timeout_ms` beside
#   it. It becomes a real defect the moment a resolver here sends
#   anything and hardcodes a number instead.
#
#   `http_only` is a cookie attribute parsed from Set-Cookie and exposed
#   as a `pub` field. Parsing it and offering it IS the library's whole
#   job; enforcing it is the caller's, and there is no caller yet.
net/dns/src/lib.rs:attempts
net/httpclient/src/lib.rs:http_only

# ---------------------------------------------------------------------------
# SECOND CATEGORY, added 2026-09-15: written in production and read by
# NOBODY -- not even a test.
#
# These were invisible until today. The gate's condition required at least
# one *test* read, so a field read by no one at all fell outside a detector
# named for exactly that defect. Found from the other direction, by a scan
# for lists of records built in a constructor: `auto_connect`, set on a
# pre-shipped IRC network in `apps/ircclient` and read nowhere.
#
# The compiler does not cover them either, and the reason is worth keeping:
# a `#[derive(Debug)]` READS every field, so `dead_code` cannot see a dead
# field on any struct that derives it. Same shape as "a `#[cfg(test)]`
# module is a use", one layer down -- `cargo clippy --all-targets` on
# `ircclient` reports zero warnings over `auto_connect`.
#
# Triaged by cluster rather than one line each: the clusters are where the
# stories are, and forty shallow notes would be the amnesty this file's
# header refuses.

# apps/automator --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/automator/src/main.rs:modified_at_ms

# apps/camera --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/camera/src/main.rs:current_frame
apps/camera/src/main.rs:recording_id

# apps/colorpicker --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/colorpicker/src/main.rs:pick_x
apps/colorpicker/src/main.rs:pick_y

# apps/diskimager --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/diskimager/src/main.rs:source_drive_id
apps/diskimager/src/main.rs:target_drive_id

# apps/filesearch --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/filesearch/src/main.rs:is_searching

# apps/hexeditor --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/hexeditor/src/main.rs:last_match

# apps/imageviewer --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/imageviewer/src/main.rs:directory

# apps/installer --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/installer/src/lib.rs:step_name

# apps/ircclient --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/ircclient/src/main.rs:topic_set_by

# apps/musicplayer --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/musicplayer/src/main.rs:track_number

# apps/paint --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/paint/src/main.rs:active_slider

# apps/pdfviewer --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/pdfviewer/src/main.rs:pdf_version

# apps/photomanager --
# Seven EXIF fields the parser extracts and the window never shows: focal
# length, whether the flash fired, GPS altitude, the writing software, the
# copyright, the exposure program and the exposure bias. Same defect as the 39
# ignored command-line options reported to lane B -- work done and discarded --
# and the same choice: show them, or stop reading them. Showing is the better
# half here, since the bytes are already decoded and a photo manager that knows
# your flash fired and will not say so is the stranger of the two.
apps/photomanager/src/main.rs:copyright
apps/photomanager/src/main.rs:exposure_bias
apps/photomanager/src/main.rs:exposure_program
apps/photomanager/src/main.rs:flash_fired
apps/photomanager/src/main.rs:focal_length_mm
apps/photomanager/src/main.rs:gps_altitude
apps/photomanager/src/main.rs:software

# apps/rssreader --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/rssreader/src/main.rs:last_attempt

# apps/spreadsheet --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/spreadsheet/src/main.rs:is_cut

# apps/sysmonitor --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/sysmonitor/src/main.rs:process_count
apps/sysmonitor/src/main.rs:running_count

# apps/terminal --
# `bracketed_paste` is set by DECSET 2004 and `blink` by SGR 5. Neither can be
# applied: this terminal has no paste path at all -- it copies a selection out
# and nothing pastes in -- and `blink` has no timer behind the renderer. Both
# are RIGHT to record rather than drop: a program has asked for a mode, and
# 'we forgot you asked' is worse than 'not yet'. Revisit when paste exists.
# `blink` additionally needs a decision about whether this terminal blinks at
# all, which is a taste question as much as a technical one.
apps/terminal/src/main.rs:blink
apps/terminal/src/main.rs:bracketed_paste

# apps/tmux --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/tmux/src/main.rs:copy_end

# apps/torrent --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/torrent/src/main.rs:completed_time

# apps/whiteboard --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
apps/whiteboard/src/main.rs:marquee

# gui/desktop --
# Five shell fields. `low_battery_warned` is the one to look at first and is
# probably a live bug: a flag whose whole purpose is 'do not warn twice', read
# by nothing, either warns every tick or never warns -- and which of those it
# is cannot be told from the field. The rest -- `ctrl_held`, `trusted`,
# `last_snapshot`, `last_tick_ms` -- sit in modules that are themselves islands
# in scripts/orphan-modules-baseline.txt, so they are downstream of that ledger
# rather than separate work.
gui/desktop/src/bluetooth.rs:trusted
gui/desktop/src/file_drop.rs:ctrl_held
gui/desktop/src/power.rs:low_battery_warned
gui/desktop/src/resmon.rs:last_snapshot
gui/desktop/src/window_peek.rs:last_tick_ms

# gui/toolkit --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
gui/toolkit/src/modal.rs:has_validator

# net/httpclient --
# Not yet triaged individually. Each is a value computed and discarded, and
# none is on a path a user can currently reach.
net/httpclient/src/lib.rs:expires

# net80211 --
# Parsed out of an EAPOL frame's header where the parser then uses the slice
# length instead. Harmless, and worth keeping while the frame layout is being
# read against the standard: a field named in the spec and absent from the
# struct is harder to check against the spec than one present and unused.
net80211/src/supplicant.rs:key_data_len
net80211/src/supplicant.rs:mic_algo
