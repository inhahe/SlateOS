{
    "sha": "a8cebf3d1",
    "date": "2026-09-17T17:04:56+00:00",
    "boot_number": null,
    "baselined_failures": [
        "our own userland runs at all (ring 3)",
        "pty ^C signal delivery (ring 3)",
        "CPython interactive REPL over a pty (ring 3)"
    ],
    "note": "Set from release boot a8cebf3d1 (2026-09-17T17:04:56Z): BOOT_OK after 201s, release/none on QEMU TCG, clean tree. THIS ONE DID NOT PASS, and that is deliberate -- read on before trusting it. It failed exactly three ring-3 self-tests and nothing else: (1) 'our own userland runs at all', (2) 'pty ^C signal delivery', (3) 'CPython interactive REPL over a pty'. All three are lane B's fixtures, all three are filed (requests/a-b-libc-execl-passes-a-null-path-to-execve.md and requests/a-b-ctest-pty-races-its-own-child-the-pty-is-fine.md), and none is a kernel defect: the first two were diagnosed to libc's execl passing a NULL path to execve and to a fixture race where the parent writes ^C before the child is ever scheduled. WHY THE CONVENTION CHANGED. The previous note required '=== Boot test PASSED ===', which no boot can produce while those three fail. The gate therefore went red on every push and the only response was ALLOW_STALE_RELEASE=1 -- so the flag became the default path with a ritual attached, and its presence in the history stopped carrying any information. A reader could no longer tell a routine bypass from a genuine wave-through. Baselining the three by name restores that: the gate is live again, and the flag means something. This is a triage queue, not an amnesty. SCOPE, CHECKED. This field is read by scripts/check-release-staleness.py and scripts/hooks/pre-push, and by nothing else. merge-readiness.py's last_green_boot() reads bench/boot-history.jsonl, not this file, so the automated ratchet is untouched and still refuses this boot. That separation is what makes the relaxation safe here and unsafe in boot-test.sh's verdict, where 'clean' is read by three lanes (design-decisions 951). RETIREMENT, PER NAME, NOT PER FILE. Each name above is retired when its request closes. If lane B fixes one and it still appears here, this baseline has begun describing a tree that moved on (boot-test.sh:4657) -- so a boot in which a LISTED fixture passes should be treated as a reason to prune this note, not as a reason to keep it. A fourth failure of any kind means this marker does not cover the run and must not be advanced from it. Supersedes 2de15d6f6 (2026-09-12), which was 137 kernel-touching commits behind against a threshold of 100."
}
