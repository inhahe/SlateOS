[package]
name = "jless"
version = "0.1.0"
edition = "2024"

[dependencies]
# Both messages name the JSON or YAML file being viewed, from a path given
# on the command line.
quoting = { path = "../quoting" }
