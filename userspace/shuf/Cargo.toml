[package]
name = "shuf"
version = "0.1.0"
edition = "2024"
description = "OurOS shuf/factor/numfmt - randomization and number tools"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
