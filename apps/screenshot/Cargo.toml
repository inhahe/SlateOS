[package]
name = "screenshot"
version = "0.1.0"
edition = "2024"
description = "SlateOS Screenshot — screen capture utility with region selection"
[dependencies]
guitk = { path = "../../gui/toolkit" }
