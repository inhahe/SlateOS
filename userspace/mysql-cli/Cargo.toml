[package]
name = "mysql-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The database name in the dump header and the mysqladmin subcommand are
# argv words.
quoting = { path = "../quoting" }
