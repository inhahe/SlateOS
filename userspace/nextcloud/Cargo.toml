[package]
name = "nextcloud"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `other` into "occ: unknown command {}. Run 'occ list' for
# help.". That value is an argv word, so it is whatever the user typed and
# not text this program chose.
quoting = { path = "../quoting" }
