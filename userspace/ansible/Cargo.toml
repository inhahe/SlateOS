[package]
name = "ansible"
version = "0.1.0"
edition = "2024"
[dependencies]
notimpl = { path = "../notimpl" }
# ansible names hosts, playbooks and roles, taken from the inventory and
# from playbook YAML on disk.
quoting = { path = "../quoting" }
