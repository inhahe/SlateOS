[package]
name = "gitea"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `other` into "gitea: unknown command {}". That value is an
# argv word, so it is whatever the user typed and not text this program
# chose.
quoting = { path = "../quoting" }
