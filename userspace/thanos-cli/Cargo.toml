[package]
name = "thanos-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# thanos-cli names blocks, buckets and tenants, identifiers written into
# object storage by another component.
quoting = { path = "../quoting" }
