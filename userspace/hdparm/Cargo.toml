[package]
name = "hdparm"
version = "0.1.0"
edition = "2024"
description = "OurOS hdparm - disk parameter tool (hdparm/sdparm)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
