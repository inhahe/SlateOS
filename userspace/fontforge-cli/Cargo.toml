[package]
name = "fontforge-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The script path and the command string it executes are argv words echoed
# back.
quoting = { path = "../quoting" }
