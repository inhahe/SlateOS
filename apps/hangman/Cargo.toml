[package]
name = "hangman"
version = "0.1.0"
edition = "2024"

[dependencies]
guitk = { path = "../../gui/toolkit" }
# The window. Without it `main` was `let _app = HangmanApp::new();` -- it drew
# a picture of a keyboard that nothing could click, into a buffer nothing
# displayed, and exited.
oswindow = { path = "../../gui/window" }

[lints]
workspace = true
