[package]
name = "conan-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The package pattern searched for and the rejected command name are argv
# words.
quoting = { path = "../quoting" }
