{
    "sha": "2de15d6f6",
    "date": "2026-09-12T01:57:00+00:00",
    "boot_number": null,
    "note": "Set from an actual release-profile boot test (run b6mifed3b, 2026-09-12: BENCH_OK after 150 s, total 2444 s, '=== Boot test PASSED ==='). Supersedes 4bd25f390, which the gate had overshot by 105 kernel-touching commits against a threshold of 100 -- and which could not be cleared for hours because the release boot the gate demands was itself refused by other gates, five runs in a row (shellcheck inherited through a merge, an open-questions entry I filed below '# Resolved', and a clippy denial in apps/launcher's test target). READ THIS BEFORE TRUSTING THE SHA: bench/boot-history.jsonl records this run with dirty=true, because two commits landed while it was running. That flag is honest and the SHA is still sound, verified rather than asserted: git diff 2de15d6f6..d5c357ac5 touches exactly scripts/check-cfg-unix.py and scripts/hooks/pre-push, and nothing under kernel/ or bench/, no .rs, no Cargo.toml or Cargo.lock. Neither file is compiled, so the kernel image that booted is exactly 2de15d6f6's. The lapse was mine and is worth recording next to the number it qualifies: I had deliberately held edits during every other run today for exactly this reason, then committed twice during this one. It cost a clean record rather than a wrong one. Note also that scripts/merge-readiness.py's last_green_boot() will NOT accept this record, by design -- it skips any dirty row, on the grounds that a boot against uncommitted edits does not identify what was tested. That divergence is intended: the automated ratchet stays strict, while this curated marker can carry a reasoned judgement with its evidence. If the two ever need to agree, re-run clean."
}
