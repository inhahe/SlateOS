[package]
name = "oxipng-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `f` into "oxipng: Optimizing {} (RGBA, 1920x1080)". That
# value is an argv word, so it is whatever the user typed and not text this
# program chose.
quoting = { path = "../quoting" }
