[package]
name = "redpanda-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# redpanda-cli names topics, groups and brokers, identifiers created by any
# client with access to the cluster.
quoting = { path = "../quoting" }
