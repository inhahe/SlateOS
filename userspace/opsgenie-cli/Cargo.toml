[package]
name = "opsgenie-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# opsgenie-cli names alerts, teams and schedules, identifiers created on the
# service side.
quoting = { path = "../quoting" }
