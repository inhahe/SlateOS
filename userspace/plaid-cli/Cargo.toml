[package]
name = "plaid-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The institution search query and the rejected command name are argv words.
quoting = { path = "../quoting" }
