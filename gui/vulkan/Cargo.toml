[package]
name = "vkloader"
version = "0.1.0"
edition = "2024"
description = "SlateOS Vulkan loader — the contract between an application and the graphics drivers installed on the machine"

[dependencies]

[lints]
workspace = true
