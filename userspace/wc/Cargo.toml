[package]
name = "wc"
version = "0.1.0"
edition = "2024"
description = "SlateOS word, line, character count utility"
[dependencies]
# wc names each input file, and it is routinely handed a whole directory via
# a shell glob.
quoting = { path = "../quoting" }
