[package]
name = "sox"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# sox names its input and output audio files, both from the command line.
quoting = { path = "../quoting" }
