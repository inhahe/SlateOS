[package]
name = "pstree"
version = "0.1.0"
edition = "2024"
description = "SlateOS pstree utility"
[dependencies]
# It interpolates `s` into "pstree: user {} not found". That value is an
# argv word, so it is whatever the user typed and not text this program
# chose.
quoting = { path = "../quoting" }
