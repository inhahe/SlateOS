[package]
name = "nft"
version = "0.1.0"
edition = "2024"
description = "OurOS nft - firewall configuration (nft/iptables/ip6tables)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
