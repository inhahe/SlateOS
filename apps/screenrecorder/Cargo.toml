[package]
name = "screenrecorder"
version = "0.1.0"
edition = "2024"

[workspace]

[dependencies]
guitk = { path = "../../gui/toolkit" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
opt-level = "z"
lto = true
strip = true
