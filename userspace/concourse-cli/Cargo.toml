[package]
name = "concourse-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# concourse-cli names pipelines, jobs and resources, defined in YAML that
# this tool did not write.
quoting = { path = "../quoting" }
