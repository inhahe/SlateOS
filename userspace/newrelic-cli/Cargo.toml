[package]
name = "newrelic-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# newrelic-cli names applications, accounts and alert policies, identifiers
# created on the service side.
quoting = { path = "../quoting" }
