[package]
name = "monctl"
version = "0.1.0"
edition = "2024"
description = "OurOS monitor/display control — power off, brightness, DPMS"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
