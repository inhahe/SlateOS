[package]
name = "mupdf-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The PDF being opened is a path from argv, and the rejected mutool command
# name is another argv word.
quoting = { path = "../quoting" }
