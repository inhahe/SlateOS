[package]
name = "scaleway-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# scaleway-cli names instances, volumes and images, identifiers the user
# chose or the API returned.
quoting = { path = "../quoting" }
