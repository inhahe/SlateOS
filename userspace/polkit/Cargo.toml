[package]
name = "polkit"
version = "0.1.0"
edition = "2024"
description = "OurOS polkit - authorization framework (polkitd/pkexec/pkaction/pkcheck)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
