[package]
name = "etcd-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# etcd-cli names keys and values, and a key in etcd is arbitrary bytes
# written by any client with access.
quoting = { path = "../quoting" }
