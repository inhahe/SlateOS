

make sure it's clear in ..\os\roadmap-detailed.md that any non-critical parts of the OS (and that don't take too much memory (if fastpy itself even takes much memory like cpython does?)) should be written in Python. (we use fastpy which executes python at speeds up to twice as fast as C++). if we don't have os function bindings in fastpy somehow so that we can use it, then i guess we need to add those too?

actually, modify that instruction a bit to include that python shouldn't be used where it would noticeably slow down performance

  [DONE 2026-08-14 — both the original instruction and this amendment landed
   together in roadmap-detailed.md.

   1. "Design Principles (non-negotiable)" gained a bullet plus a new
      "Implementation Language Policy (operator-directed)" subsection stating
      that Python-via-fastpy is the *default* for everything that is not
      kernel-space or hot-path — userspace tools, services, utilities, the
      package manager, the installer, settings/config UIs, build scripts and
      the desktop apps — and that writing a new userspace component in Rust
      needs a reason from an explicit five-item exclusion list, stated in the
      commit. Framed as a default rather than a preference, because the choice
      had been getting made ad-hoc per component.

   2. Your memory question, answered in the doc: **no, fastpy does not cost
      what CPython costs.** On SlateOS fastpy builds in *pure mode* — the
      program is AOT-compiled to machine code and statically linked against a
      seven-TU C runtime plus our own libc.a; `libpython` is not linked and
      `cpython_bridge.c` is replaced by `bridge_stub.c`. There is no
      interpreter image, no import machinery, no .pyc cache, no per-process
      module dicts. A real fastpy program links to a ~2.9 MB static ET_EXEC and
      its runtime memory is just its own heap plus refcount/GC bookkeeping.
      The doc also notes the 2.9 MB is *binary size* — mostly demand-paged
      text, per distinct program rather than per process.

   3. Your speed figure, stated precisely rather than as a slogan, since agents
      act on this doc: 0.2x-2.3x of MSVC /O2 C++ across fastpy's own benchmark
      set (faster than C++ on tight loops 0.5x, function calls 0.2x, recursion
      0.6x, attribute access 0.5x; ~2x slower on allocation-heavy pointer
      chasing), and 3x-173x faster than CPython.

   4. This amendment is exclusion reason #2, "Python would noticeably slow it
      down", covering both the performance-critical subsystem table *and* any
      perceptible slowdown — user-visible latency budgets, throughput paths, or
      an inner loop run often enough for a 2x to matter. It points at the known
      shape of the gap (object churn and pointer chasing, not scalar work) so
      the test is "does this churn objects in a hot loop?" rather than "is this
      component important?", and it cuts both ways: a profile showing real cost
      overrides the default, and often only the hot *function* needs to move,
      not the whole program.

   5. On your "if we don't have os function bindings, we need to add those" —
      we do have them, and they are native and bridge-free: codegen lowers
      `os.*` to `fastpy_os_*` runtime C, which calls our POSIX libc, which
      dispatches to the kernel SYS_* syscalls. No CPython on any of those
      paths. ~56 functions are implemented and on-target tested, plus the
      built-in `open()`/file objects and a CPython-free `pathlib.Path`; that is
      what the 60+ `services/fastpy-*` binaries are built on. So Phase 0 gained
      a "fastpy language bindings for OS APIs" subsection recording what exists
      and, more usefully, what does not:
        - The real gap is that Python has **no binding for the native
          (non-POSIX) SlateOS API at all** — capability handles, channel IPC,
          the IOCP event loop / io_uring, service discovery + RPC, shared
          memory and futexes, structured logging. That, not performance, is
          why native-side components still have to be Rust today, so it is now
          tracked as roadmap work rather than being an invisible constraint.
        - Remaining POSIX gaps listed in dependency order: socket/select,
          termios/pty, mmap, posix_spawn/subprocess, on-target threading.
        - GUI toolkit bindings, blocked on the toolkit, without which the apps
          already specified as Python/fastpy cannot actually be written in it.

   Two stale statuses corrected in passing, both verified rather than assumed:
   Phase 0's fastpy build-system item is [-] in progress (not [ ]), and §4.8's
   "fastpy compiler" item now distinguishes cross-compiling to SlateOS (works
   today, in use) from hosting the compiler on SlateOS (needs CPython first).]

do the speed benchmarks on fastpy again since OS had to do some fixes.
  [FOR THE FASTPY SESSION, NOT THIS ONE — the benchmarks live in the fastpy repo
   (`D:\visual studio projects\fastpy`) and measure its compiler/runtime; nothing
   in this repo can move those numbers. Flagging rather than silently skipping.]

tell opus in OS:
I told Fable 5 to optimize fastpy, so you may want to re-port whatever's necessary to assimilate the optimizations. Here's what Fable 5 said:
"""
Summary

I worked through both phases of the optimization task, committing before each change so everything is revertible. Baseline was tagged at commit ae2da03.
Phase 1 — Generated-code optimization strategies

I analyzed the fundamental strategies (type-specialized codegen, unboxing via --typed/--int64, inlining, per-class free-lists, adaptive GC, LLVM -O2/-O3). The verdict: the generated code is already heavily optimized — the runtime (objects.c free-list allocator, gc.c adaptive threshold, acyclic-class GC-skip) and the type-inference prescan are mature. The one tunable I added is a FASTPY_OPT env knob (default 2) wiring the backend opt level through both the LLVM pass pipeline and codegen. I A/B-tested -O3; it showed no reliable win (regressions amid measurement noise), so -O2 stays the default. Micro-benchmark deltas below ~10% aren't provable on this machine — the compiled programs run in 7–40ms and are dominated by ~7ms subprocess startup + Windows scheduling noise. So I pivoted the substantive work to the highly measurable compile-time hotspots.
Phase 2 — Compiler CPU hotspots (the real wins)

Profiled with cProfile and found two dominant costs:
Optimization	What it was	Before	After	Improvement
MSVC env caching (toolchain.py, d83b6f5)	Re-ran vcvars64.bat on every link to find link.exe + env (~2.1s each)	2400 ms compile wall	~200 ms	−92% end-to-end compile
↳ _link_windows specifically		2148 ms	141 ms	−93%
Hoisted tree walks (codegen.py, dbb6b1f)	Two nested full-AST walks re-run per candidate receiver in _detect_class_container_attrs; they were loop-invariant	7.51 s (179 ms/call)	1.08 s (26 ms/call)	−86% on that function (Richards)

The MSVC environment is now captured once and cached both in-process and on disk (runtime/_msvc_env_cache.json, keyed on vcvars path + mtime); link.exe is invoked directly with that env, falling back to the old batch method if the cache fails. The codegen fix precomputes _ctor_first/_ctor_fixpoint maps once before the per-receiver loop instead of walking the whole tree each iteration — semantics are byte-for-byte identical.
Testing

Full differential suite run twice (after each phase): 593 passed, 0 regressions (test_differential, test_comprehensive, test_full_suite, test_cpython_adapted; 4m44s). The A/B numbers above were verified by checking out the baseline files and re-measuring under identical warm-cache conditions.
Docs updated
BENCHMARK_REPORT.md — added a "Compiler Speed" section (compile −92%, codegen −86%, FASTPY_OPT note).
README.md — documented the FASTPY_OPT environment variable.
Commits (on top of baseline ae2da03, not pushed per project rules)
096babe docs: document compile-speed wins and FASTPY_OPT knob
dbb6b1f codegen: hoist loop-invariant tree walks in container-attr detection
d83b6f5 toolchain: cache MSVC env instead of re-running vcvars per link

Bottom line: compile/iteration time dropped ~92% (2.4s → ~0.2s for a typical small program) with no runtime regressions and no behavioral changes. The generated code itself was already near-optimal, so the headline gains are in developer iteration speed rather than output performance.
"""
  [READ 2026-08-06 — nothing to re-port into this repo. I checked: the OS tree
   vendors no copy of the fastpy compiler. `git ls-files | grep fastpy` outside
   `services/` returns exactly one file, `scripts/convert-fastpy-embeds.py`, and
   the ~200 `services/fastpy-*` directories are *generated outputs* (compiled
   ring-3 test programs), not compiler source. So all three of Fable 5's commits
   (d83b6f5 toolchain MSVC-env cache, dbb6b1f codegen hoisted tree walks, 096babe
   docs) land wholly inside `D:\visual studio projects\fastpy` and this repo picks
   them up for free the next time it invokes fastpy — the −92% is on *our* build
   times too, since every `services/fastpy-*` rebuild pays the old ~2.1 s
   vcvars64 cost per link.
   One thing worth knowing here: `FASTPY_OPT` defaults to 2. If a fastpy-built
   OS component ever needs a different backend opt level, that is now the knob.]

--

the crate is ~15k+ lines with 477 passing tests and is quite mature now. Most calendar time is rate-limit windows, not active work. If you ever want the faithful C++ cross-compile instead, the crate is isolated enough to swap once a C++/slateos toolchain exists (which Chromium/WINE will need anyway) — but that path is currently prerequisite-blocked.

". This is a real, in-scope bash-superset gap (the goal is bash compatibility, and line N: is strictly more useful for debugging). "
The reason I wanted you to port oils is because of its superset capability. Can you port all of it?

  [ANSWERED 2026-08-06 — yes, with one distinction worth making, because "all of
   oils" is two different things shipped in one binary:

   1. OSH — the bash-compatible half. That is what `userspace/oils` already is,
      and it is the part your superset motive actually wants. Today: ~110k lines
      of Rust, 458 differential corpus cases + ~1,390 unit tests, every case
      compared byte-for-byte against real bash 5.2.37 (stdout, stderr, exit
      status). What remains is a long tail of *measured* divergences — one
      escape, one redirect shape, one builtin flag at a time — not missing
      subsystems. Each costs ~5–20 min, so the tail is bounded work, not an open
      research problem, and it shrinks every session.

   2. YSH — oils' *new* language (typed Int/Float/List/Dict values, `proc`/`func`,
      the `=` expression dialect, `json`/`json8`, blocks as arguments,
      `var`/`setvar`/`const`, `try`/`_error`/`_status`). It shares oils' runtime
      but is a separate language, and it adds nothing to bash compatibility.
      Also portable: it is a second front end (lexer → parser → evaluator) over
      the interpreter and value model I already have, exposed as a `ysh` binary
      the way oils exposes one binary under two names. Order-of-magnitude: a few
      focused active hours to a couple of calendar days.

   One caveat on method. I do not translate oils' Python line-by-line; the port
   is differential — measure bash, read bash's C source to *explain* the
   measurement, then freeze it as a corpus case. For OSH that reference exists
   locally (`/d/refsrc/bash-5.2`). For YSH there is no bash to measure, so the
   reference has to become oils' own source + its spec-test suite, and neither is
   checked out here. If you want YSH, drop the oils repo somewhere on disk (or
   say the word and I'll fetch it) — otherwise I'd be reimplementing a language
   from memory, which is exactly the failure mode the differential method exists
   to avoid.

   Recommendation, which matches your unfinished note below: finish the OSH
   surface first — it is what makes the shell a bash *superset*, and it is the
   one on the critical path for init scripts, build scripts and the installer —
   then add YSH as a second dialect once its reference is available.]

I have an idea: Continue porting a bash-compatible shell from oils, then once we get our C++ toolchain in, you can 

make draggable margins' and sliders' clickable regions wider than the norm
  [DONE 2026-07-20 — documented in roadmap-detailed.md §3.5 "Generous hit-regions
   for draggable control handles (sliders too)", extending the existing "Forgiving
   drag margins" item to cover slider thumbs/tracks and any small draggable handle.]

write in roadmap-detailed.md that the file explorer can allow you to rearrange the order of files and persist it (but when order by whatever is enabled it'll temporarily order by that)
  [DONE 2026-07-20 — documented in roadmap-detailed.md §4.1 "Manual (custom) file
   ordering with persistence", incl. the precedence rule that column sorts are a
   non-destructive temporary override of the persisted manual order.]