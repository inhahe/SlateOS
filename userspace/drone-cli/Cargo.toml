[package]
name = "drone-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# drone-cli names repositories, builds and secrets, identifiers created on
# the service side.
quoting = { path = "../quoting" }
