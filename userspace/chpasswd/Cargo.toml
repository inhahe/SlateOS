[package]
name = "chpasswd"
version = "0.1.0"
edition = "2024"
description = "OurOS chpasswd - batch password change (chpasswd/passwd)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
