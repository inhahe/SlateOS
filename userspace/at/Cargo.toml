[package]
name = "at"
version = "0.1.0"
edition = "2024"
description = "OurOS at — schedule one-time command execution at a specified time"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
