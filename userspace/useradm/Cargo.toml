[package]
name = "useradm"
version = "0.1.0"
edition = "2024"
description = "OurOS user account management — useradd, userdel, passwd, usermod"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
