[package]
name = "yacc"
version = "0.1.0"
edition = "2024"
description = "OurOS yacc - parser generator (yacc/bison)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
