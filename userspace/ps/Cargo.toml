[package]
name = "ps"
version = "0.1.0"
edition = "2024"
description = "SlateOS process listing utility"
[dependencies]
# `/proc` parsing lives in `procinfo`, shared with `userspace/htop` and
# `apps/procexplorer`. See `known-issues.md` ->
# TD-B-TEN-PROC-PARSERS-IN-USERSPACE-AND-ONE-CRATE.
procinfo = { path = "../../procinfo" }
