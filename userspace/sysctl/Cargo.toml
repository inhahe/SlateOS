[package]
name = "sysctl"
version = "0.1.0"
edition = "2024"
description = "OurOS sysctl — view and modify kernel parameters"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
