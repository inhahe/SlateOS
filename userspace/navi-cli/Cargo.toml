[package]
name = "navi-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# navi-cli names cheatsheets and snippets, read out of cheat files that are
# commonly fetched from a shared repository.
quoting = { path = "../quoting" }
