[package]
name = "vllm-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# vllm-cli names models and served model identifiers, taken from the command
# line and from a downloaded model config.
quoting = { path = "../quoting" }
