[package]
name = "df"
version = "0.1.0"
edition = "2024"
description = "OurOS disk free space utility"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
