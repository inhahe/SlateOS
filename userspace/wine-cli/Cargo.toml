[package]
name = "wine-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# The Windows program it starts and the registry file regedit imports are
# paths from argv.
quoting = { path = "../quoting" }
