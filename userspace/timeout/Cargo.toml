[package]
name = "timeout"
version = "0.1.0"
edition = "2024"
description = "OurOS timeout/nohup/nice/renice process control"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
