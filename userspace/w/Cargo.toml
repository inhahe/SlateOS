[package]
name = "w"
version = "0.1.0"
edition = "2024"
description = "OurOS w - show who is logged in and what they are doing"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
