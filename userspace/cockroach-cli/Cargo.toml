[package]
name = "cockroach-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# cockroach-cli names databases, tables and nodes, identifiers that come
# from the command line or from the cluster's own reply.
quoting = { path = "../quoting" }
