[package]
name = "networkd-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The rejected subcommand names are the argv words themselves.
quoting = { path = "../quoting" }
