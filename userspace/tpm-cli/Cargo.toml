[package]
name = "tpm-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
# It interpolates `cap` into "tpm2_getcap: capability {} listed". That value
# is an argv word, so it is whatever the user typed and not text this
# program chose.
quoting = { path = "../quoting" }
