[package]
name = "guiremote"
version = "0.1.0"
edition = "2024"
description = "SlateOS remote-desktop protocol — serialises guitk RenderCommand streams"
[dependencies]
guitk = { path = "../toolkit" }

[dev-dependencies]
# Integration tests construct RenderCommands directly to drive the scene
# protocol the way a compositor would.
guitk = { path = "../toolkit" }
