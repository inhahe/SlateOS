[package]
name = "lex"
version = "0.1.0"
edition = "2024"
description = "OurOS lex - lexical analyzer generator (lex/flex)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
