[package]
name = "wget"
version = "0.1.0"
edition = "2024"
description = "SlateOS HTTP file download utility"
[dependencies]
# The output file name in the final `saved` line is derived from the *URL*
# -- a server chooses it through Content-Disposition, so it is remote input.
quoting = { path = "../quoting" }

# Inherit the workspace lint configuration. This crate had none at all: it was
# not in the `[lints] workspace = true` set and set no `#![deny]`/`#![warn]` of
# its own, so `unwrap_used`, `expect_used`, `indexing_slicing` and
# `arithmetic_side_effects` were silently off over code that
# parses HTTP responses, and derives an output filename from a
# server-supplied header.
# See known-issues.md `TD-B-USERSPACE-CRATES-DO-NOT-INHERIT-THE-WORKSPACE-LINTS`.
[lints]
workspace = true
