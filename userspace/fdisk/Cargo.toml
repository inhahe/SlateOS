[package]
name = "fdisk"
version = "0.1.0"
edition = "2024"
description = "OurOS partition table manipulator (GPT/MBR)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
