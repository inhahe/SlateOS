[package]
name = "diskutil"
version = "0.1.0"
edition = "2024"
description = "OurOS disk utility — format, info, check, benchmark"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
