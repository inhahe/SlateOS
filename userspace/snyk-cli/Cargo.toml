[package]
name = "snyk-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# snyk-cli names projects, packages and vulnerability identifiers read out
# of a manifest and out of downloaded advisory data.
quoting = { path = "../quoting" }
