[package]
name = "texlive-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# The package search term and the tlmgr action are argv words.
quoting = { path = "../quoting" }
