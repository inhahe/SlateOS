[package]
name = "wget"
version = "0.1.0"
edition = "2024"
description = "OurOS HTTP file download utility"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
