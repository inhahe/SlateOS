[package]
name = "modprobe"
version = "0.1.0"
edition = "2024"
description = "OurOS modprobe - kernel module tools (modprobe/insmod/rmmod/lsmod/modinfo/depmod)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
