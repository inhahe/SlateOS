[package]
name = "mesg"
version = "0.1.0"
edition = "2024"
description = "OurOS mesg - control/send terminal messages (mesg/write/talk)"

[workspace]

[dependencies]

[profile.release]
opt-level = "s"
lto = false
panic = "abort"
strip = "symbols"

[profile.dev]
panic = "abort"
