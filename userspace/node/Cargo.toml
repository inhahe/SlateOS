[package]
name = "node"
version = "0.1.0"
edition = "2024"
[dependencies]
# npm and corepack echo the rejected command name back, and that word is
# whatever the user typed after the program name.
quoting = { path = "../quoting" }
