[package]
name = "civo-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
notimpl = { path = "../notimpl" }
# civo-cli names clusters, instances and volumes, identifiers the user chose
# or the API returned.
quoting = { path = "../quoting" }
